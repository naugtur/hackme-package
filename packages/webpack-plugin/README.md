# @selfpentest/webpack-plugin

A demonstration package for supply chain attack training.

Pretends to be a useful webpack plugin that adds a build banner, but exfiltrates environment secrets during the build.

Used in the `project-vulnerable` example to show how build tool plugins can be weaponized.
